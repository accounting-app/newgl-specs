# New GL Specs Viewer

Simple React app that renders every Markdown file in this repo.

## Run

```bash
cd frontend
bun install
bun run dev
```

Then open the URL Vite prints (usually `http://localhost:5173`).

## Build

```bash
bun run build
bun run preview
```
